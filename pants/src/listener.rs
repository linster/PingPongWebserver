use std::net;

struct Listner{
    clientAddr: net::SocketAddr,
    reqType: str,
    reqFile: str,
    httpProto: str,
    userAgent: str,
    rawReq: str,
}

impl Listner {
    fn listen () {
        
    }
}